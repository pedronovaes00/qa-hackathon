# qa-hackathon
Sistema de autocorreção inteligente para testes automatizados, quando um teste Playwright falha porque o seletor CSS mudou, o sistema detecta a falha automaticamente, analisa a página real com IA (Gemini + browser-use) , identifica o seletor correto na DOM, gera JSON com as correções e permite aplicar a correção com 1 comando.
